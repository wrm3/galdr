---
description: Gemini CLI quick reference — auth, checkpointing, config, extensions, cross-agent use.
---
# /g-cli-gemini

Activates `g-skl-cli-gemini`. Use when running Gemini agents from the terminal.

```
/g-cli-gemini    → load g-skl-cli-gemini context
```
