---
description: Gemini CLI quick reference — auth, checkpointing, config, extensions, cross-agent use.
---
# /g-gemini-cli

Activates `g-skl-gemini-cli`. Use when running Gemini agents from the terminal.

```
/g-gemini-cli    → load g-skl-gemini-cli context
```
