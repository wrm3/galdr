---
description: Cursor CLI quick reference — Agent/Plan/Ask/Debug modes, Cloud Agent handoff, session management.
---
# /g-cursor-cli

Activates `g-skl-cursor-cli`. Use when running Cursor agents from the terminal.

```
/g-cursor-cli    → load g-skl-cursor-cli context
```
