Activate the **g-upgrade** skill.

Audits the entire .galdr/ folder structure, heals missing files/folders, migrates task YAML schemas, syncs all indexes, fills placeholders, and bumps galdr_version in .identity.

Default mode: dry-run (reports what would change). Pass 'apply' to execute all fixes.