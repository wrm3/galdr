# PRDs (`prds/`)

Add one markdown file per product requirements document. Filenames and titles should use `{placeholders}` until you replace them with real values.

Link each PRD from the **Deliverable Index** in `PLAN.md`.

Create new PRDs with your planning workflow (e.g. `@g-plan`).
