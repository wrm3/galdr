{
  "name": "galdr-desktop",
  "version": "1.0.0",
  "private": true,
  "description": "galdr desktop app — Tauri wrapper for the galdr web UI",
  "scripts": {
    "tauri": "tauri",
    "dev": "tauri dev",
    "build": "tauri build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  },
  "dependencies": {
    "@tauri-apps/api": "^2"
  }
}
