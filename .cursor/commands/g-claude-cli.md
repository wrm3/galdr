---
description: Claude Code CLI quick reference — headless flags, session continuation, MCP config, permissions, overnight use.
---
# /g-claude-cli

Activates `g-skl-claude-cli`. Use when running Claude Code from the terminal.

```
/g-claude-cli    → load g-skl-claude-cli context
```
